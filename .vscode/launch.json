{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Web App",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/web_stuff",
            "cwd": "${workspaceFolder}"
        }
    ]
}
